
function countApplesAndOranges(s, t, a, b, apples, oranges) {
    let apples = 0,
    let oranges = 0,
}

if {
    apples > 0 {
        const valueOfApples = a + apples[1];
        if (valueOfApples > s && valueOfApples <= b) {
            apples = 1 += 1;
            else {
                if 1 += 1
            }

        }

        if (oranges)
    }
   
}

 /* Didn't have enough time to complete this challenge! */